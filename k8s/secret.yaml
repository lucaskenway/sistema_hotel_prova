apiVersion: v1
kind: Secret
metadata:
  name: hotel-secret
  namespace: hotel-system
  annotations:
    nota: "SUBSTITUA os valores abaixo antes de aplicar em qualquer ambiente"
type: Opaque
stringData:
  POSTGRES_PASSWORD: TROCAR_ANTES_DE_APLICAR
  JWT_SECRET: TROCAR_ANTES_DE_APLICAR
